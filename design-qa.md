# 2026-08-22 主页视觉 QA：Antigravity「舞台聚光」落地

## 验收范围

本轮只改主页，不改第一人称汉堡制作流程。视觉真值来自：

- `C:/Users/KID/.gemini/antigravity/brain/3bbf14aa-44f1-4e6a-938c-25f6c4d4d9d8/design_handoff.md`
- 同目录桌面汉堡、桌面寿司和移动汉堡定稿图。

完成线：玩家进入主页后，看到全屏主题舞台、居中持续旋转的 3D 食物、实体地台、上下三层玩法轨道和主按钮；左右只切汉堡/寿司，上下只切玩法，桌面与移动首屏无滚动、无裁切、无内容重叠。

## 已消除的问题

### [P0] 桌面被锁成手机竖条

旧版桌面主体宽度约 496px，左右大面积空白，3D 食物被窄画布裁切。现在 `.lobby-shell` 为真正的全视口舞台，1920×1080 下宽高均完整覆盖，页面 `scrollWidth/scrollHeight` 与视口一致。

### [P0] 3D 食物比例失控

旧版窄高画布让汉堡巨大到裁切。现在画布横跨舞台，模型按画布宽高比自适应缩放：移动端收紧、宽屏放大，并始终落在地台上方。左右边缘提示使用低透明度的真实 3D 对侧食物，不加载截图或贴图。

### [P0] 模式切换没有可见轨道

当前模式使用奶油色厚描边卡，上一模式和下一模式同时以半透明卡显示。点击两张幽灵卡、上下滑动或方向键都调用同一模式状态机；切换时当前卡和编号有落位回弹。

### [P1] 汉堡与寿司视觉主题割裂

汉堡使用 `#F5A623 / #E8900A / #3D1A00`；寿司使用 `#3DBFA0 / #2A9E84 / #1A3D30`。背景、顶栏、标题、地台、模式卡、按钮和浏览器主题色同步过渡。

### [P1] 首屏操作职责不清

- 左右滑动 / 左右方向键：只切 `汉堡 ↔ 寿司`。
- 上下滑动 / 上下方向键：只切 `自由练习 ↔ 复刻对决 ↔ 双人制作`。
- 寿司主按钮显示“寿司玩法筹备中”，点击只出现提示，不跳入汉堡流程。
- 当前模式卡与“开始游戏”按钮都能进入当前汉堡玩法。

## 视觉对照与尺寸证据

- 同图对照：`output/home-stage-spotlight-2026-08-22/desktop-reference-vs-implementation.png`
- 桌面浏览器帧：`output/home-stage-spotlight-2026-08-22/desktop-browser-raw.png`
- 移动浏览器帧：`output/home-stage-spotlight-2026-08-22/mobile-browser-raw.png`
- 结构、尺寸、交互与控制台记录：`output/home-stage-spotlight-2026-08-22/browser-verification.json`

1920×1080 实测：顶栏 72px；3D 舞台 1872×514；地台 500×100；当前模式卡 520×80；CTA 320×64；页面无横向或纵向滚动。

移动验收受内置浏览器最小宽度限制，以 480×844 实测：顶栏 56px；当前模式卡 300×60；CTA 260×52；所有主体操作在 844px 首屏内，横纵滚动增量均为 0。390px 规则使用同一 `<600px` 布局，固定控件宽度均不超过 300px。

## 自动与运行时验收

- Node 全仓测试：85/85 通过。
- Gauntlet repo check：测试、空白和料理缓存链全部通过。
- `git diff --check`：通过。
- 主页缓存链：`20260822-stage2`。
- 浏览器控制台 error/warn：`[]`。
- 汉堡 → 寿司 → 汉堡、自由练习 → 复刻对决均在真实浏览器完成。

## 已知非阻断项

- Antigravity 稿中的叶片、番茄、芝麻等装饰涂鸦没有用位图补齐；遵循项目“不在游戏里放截图和图片”的约束，当前以留白和真实 3D 边缘食物保持舞台感。
- 寿司仍是主页 3D 预览，没有制作玩法。
- 旧 UI 编辑器仍含餐车时代的分组名称；正常主页不显示这些内容。

final result: passed
