# 汉堡档口双绳摆坠 Design QA

日期：2026-07-30

## 验收对象

- 视觉方向参考：`docs/art-direction/reference-flat-burger-game.png`
- 修改前动画：`output/ui-reassessment/08-weighted-motion-contact-sheet.png`
- 本轮最终动画：`output/ui-pendulum-qa/07-final-swing-contact-sheet.png`
- 本轮稳定终态：`output/ui-pendulum-qa/10-final-stable.png`
- 修改前后同屏：`output/ui-pendulum-qa/11-before-after-comparison.png`
- 本地验收地址：`http://127.0.0.1:4173/?visualQa=1&motion=pendulum3`

## 截图条件

- 浏览器：Codex 内置浏览器。
- 浏览器窗口：`2560 × 1440`，DPR `0.75`。
- 页面截图区域：`x=980, y=40, width=600, height=920`。
- 浏览器返回图片为 `1200 × 1840`，最终移动端证据裁切为 `(760, 0, 1180, 920)`。
- 稳定状态：汉堡卡片居中、档口停稳、卷帘打开、营业按钮可操作。

## 本轮问题与修复

### [P1] 下落仍像直上直下

- 问题：上一版主要只有垂直位移，横向重心变化和绳索滞后太弱。
- 修复：档口由左偏进入，在 `64%` 形成向右冲摆，在 `77%` 回摆到左侧，再用两次很短的小幅摆动归零。
- 最终幅度：起始 X `-5.4%`、第一次冲摆 X `+5.2%`、回摆 X `-3.35%`；最大旋转约 `±3°`。
- 节奏：主体 `1080ms`，镜头 `1440ms`；卷帘延迟 `930ms`、时长 `360ms`。
- 结果：前段下坠更快，第一次摆动清楚可读，停稳不拖尾。

### [P1] 两根绳在下落初段悬挂到档口下方

- 问题：最初用整张绳索图做裁切，绳索会在主体还未落下时贯穿卡片。
- 修复：同一透明素材拆成左右两个渲染实例，分别以顶部锚点为旋转原点，并用 `scaleY` 随档口下落逐步放长。
- 结果：两根绳始终从舞台顶部连接到档口铜环，没有横杆，也没有垂到档口下方。

### [P1] 左右卡片稳定时露边，切换时层级交接不可靠

- 问题：邻卡原先只平移 `88%`，稳定状态仍可能侵入主视口；运行时层级直接写入 `z-index`，容易被布局编辑器复位覆盖。
- 修复：邻卡平移改为 `104%`；轮播层级独立写入 `--map-carousel-z`；切换目标卡获得 `z=30`，离场卡退到 `z=18`，外层缓存卡为 `z=6`。
- 实测：稳定状态中左右邻卡与主视口重叠均为 `0px`；当前卡为 `z=30`，邻卡为 `z=18`。
- 结果：左右返回都有动画，中间卡片不再出现邻卡叠片或残留边缘。

## 视觉表面核对

- 字体与文案：继续使用现有中文系统黑体和项目文案，没有新增 AI 图内文字。
- 构图：只保留完整银色出餐档口；车头、车门、车轮、轮眉、道路语义均未恢复。
- 色彩：暖黄、深棕、银灰、快餐红、菜单深绿保持现有扁平插画体系。
- 人物与汉堡：厨师仍居中；汉堡和食材继续沿用现有装配方式，没有改成木偶。
- 绳索：两根细绳分别连接档口左右铜环，摆动方向与档口重心一致。

## 交互与代码验收

- 初次进入：双绳摆坠、短回摆、停稳后开卷帘。
- 点击“重播进场”：可以重复播放完整动画。
- 点击左右箭头：汉堡与寿司卡片可往返，返回汉堡会再次播放档口入场。
- 稳定状态：邻卡对主视口重叠 `0px`。
- 切换期：目标卡层级 `30`，离场卡层级 `18`。
- 浏览器控制台：`0` 条 error / warning。
- Node 自动测试：`30/30` 通过。
- `git diff --check`：通过。

## 剩余 P3

- 厨师目前仍是整张人物图。下一轮只拆厨师躯干、上臂、前臂和双手，制作“像提线木偶的人在装汉堡”；汉堡与食材本身不拆成木偶，也不改现有装配规则。
- 真机触摸滑动手感仍建议在手机上补一次体验验收，但不影响本轮桌面浏览器交付。

## 最终结论

本轮已将“垂直飘落”改为可读的双绳摆坠，并解决稳定露边和轮播层级所有权冲突。现有银色档口、人物中心和汉堡装配方向均被保留。

final result: passed
