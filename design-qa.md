# 汉堡餐车分层重制 Design QA

最后检查：2026-07-27

## 比较目标

- Source visual truth：`C:\Users\KID\.codex\generated_images\019f9d0f-8b99-7b23-933f-f070809731d9\call_C4tzYBlRvYueeVcOfGbyxy35.png`
- 浏览器最终画面：`output/burger-truck-layered/browser-final-phone.png`
- 同屏比较：`output/burger-truck-layered/reference-vs-layered-final.png`
- 分层资产总览：`output/burger-truck-layered/layer-assets-contact-sheet.png`
- 浏览器原始截图：`output/burger-truck-layered/browser-final-full.png`

## 归一化信息

- Source 原图：852 × 1847 px。
- 浏览器截图：1920 × 1080 px；页面 CSS 视口为 2560 × 1440，设备像素比为 0.75。
- 当前汉堡卡片：约 382 × 522 CSS px。
- QA 使用的手机区域裁切：400 × 620 px。
- 同屏比较画布：882 × 802 px；两侧均等比缩放，没有横向拉伸。
- 状态：餐车已完成驶入和刹停，卷帘已打开，镜头已聚焦至菜单灯箱和出餐窗口，营业与重播控件已恢复。

## Findings

当前没有未解决的 P0、P1 或 P2。

- 字体与排版：保留项目已有的首页字体栈、HUD、标题、玩法标签、营业状态和底部导航。概念图标题更大，但这是既有首页信息架构约束，属于可接受差异。
- 间距与布局节奏：整车阶段以完整车身进入卡片；最终阶段裁掉轮胎和大部分车身，只保留发光汉堡招牌、三联菜单灯箱、厨师双手、主汉堡和出餐台。左右地图箭头与底部导航没有被镜头裁掉。
- 颜色与视觉令牌：奶油白、番茄红、芥末黄、深棕、暖白灯光与蓝绿色背景一致；没有使用麦当劳标志或其他品牌图形。
- 图片质量与资产一致性：所有可见餐车元素都来自独立栅格资产，不再用单张整屏截图、CSS 圆形、CSS 汉堡或镜像裁图冒充结构。透明边缘、遮罩和 WebP 压缩未出现明显色边。
- 文案与内容：`重播进场`、`点击开门营业`、`点击关门打烊` 与现有首页状态逻辑一致。
- 状态与交互：轮胎滚动、车身刹停回弹、卷帘上升、镜头推进和三块菜单面板错峰翻转分别运行。重播期间控件隐藏，动画结束后恢复。
- 可访问性：装饰图使用空 `alt`，交互按钮保留可访问名称、焦点样式和 `aria-pressed`；项目原有 `prefers-reduced-motion` 规则继续生效。
- 响应性：当前桌面页面内部保持约 390 px 的移动端卡片宽度，最终镜头没有造成横向页面溢出，也没有裁掉持续导航控件。

## Comparison history

### Iteration 1：被用户拒绝的单图版本

- [P1] 整个餐车场景只有一张 `burger-truck-master.png`，驶入和聚焦只是对同一张图做位移与缩放；画面像会动的海报，与现有页面割裂。
- [P1] 菜单反面是同一裁图的水平镜像，不能表达真实的三联翻转灯箱。
- 修复：替换为车身、车轮、出餐窗口、卷帘、顶部招牌、菜单机械框和三张食物卡共九类独立资产；最终页面实际加载 13 个图片节点。

### Iteration 2：分层动画与镜头

- [P2] 初始实现需要确认车轮、车身、卷帘和镜头不是同一个动画。
- 修复：车轮使用独立旋转关键帧；车身使用刹停回弹；卷帘在停稳后上升；镜头最后从 1 倍推进至 2.1 倍；三块菜单面板分别延迟翻转。
- 复测：0.9 秒采样中轮胎旋转而车身仍在驶入；停稳后卷帘位移至窗口外；最终镜头矩阵约为 `scale(2.1)`。

### Iteration 3：最终近景与控件

- [P2] 需要确保最终镜头仍保留完整菜单灯箱、主汉堡和营业控件。
- 修复：镜头原点设为出餐窗口区域，最终缩放调整为 2.1；控件只在动画结束后恢复。
- 复测证据：`output/burger-truck-layered/reference-vs-layered-final.png`。菜单灯箱、汉堡招牌、厨师双手、主汉堡、重播和营业控件均可见。

## 浏览器验证

- 本地 URL：`http://127.0.0.1:4173/`
- 已测试：餐车重播、轮胎滚动、刹停回弹、卷帘开门、镜头聚焦、菜单翻转、营业开门、营业打烊、控件恢复。
- 营业按钮：`aria-pressed` 按 `false → true → false` 正常切换，可访问名称同步更新。
- 页面控制台错误：0。
- 13 个餐车图片节点全部加载完成，`naturalWidth` 与 `naturalHeight` 均有效。

## Focused region comparison

出餐窗口是本轮最重要的局部，因此使用 `reference-vs-layered-final.png` 进行同屏比较。实现保留了概念图的三层视觉顺序：发光汉堡招牌 → 三联菜单灯箱 → 厨师递出主汉堡。概念图里的大面积奶油色车身被更紧的镜头裁掉，是用户明确要求的“最终只显示局部出餐位置”，不视为设计漂移。

## Follow-up polish

- [P3] 下一轮可以加入轮胎滚动、刹停、卷帘、菜单翻页和出餐提示音。
- [P3] 若继续提高最终近景精度，可把服务窗口的汉堡再放大约 6%，但当前可读性已经通过。

final result: passed
