# 汉堡档口重落版 Design QA

日期：2026-07-30

## 对比目标

- 视觉语言真值：`docs/art-direction/reference-flat-burger-game.png`
- 本轮修改前：`output/ui-reassessment/01-current-ui.png`
- 本轮稳定终态：`output/ui-reassessment/07-final-stable.png`
- 参考与实现同屏：`output/ui-reassessment/09-reference-vs-final.png`
- 动画四阶段：`output/ui-reassessment/08-weighted-motion-contact-sheet.png`
- 本地验收地址：`http://127.0.0.1:4173/?visualQa=1&motion=weight2`

## 视口与截图说明

- 在 Codex 内置浏览器使用 `420 × 900` 移动端视口覆盖进行验收。
- 浏览器返回 `1680 × 3600` 平铺捕获；本轮证据从首个可用平铺区裁出 `560 × 1200` 画面，参考图只用于核对扁平描边、暖色、低细节密度和信息层级，不按像素克隆。
- 稳定状态为汉堡卡片居中、档口停稳、卷帘打开、营业按钮可用。

## 本轮发现与修复

### [P1] 进场过慢、近似匀速漂移

- 修改前：档口下落与停摆 `2500ms`，镜头占位 `3500ms`，多段缓慢回摆让主体缺少重量。
- 影响：用户首先感知到的是“从上面飘下来”，不是提线木偶布景受重力落位。
- 修复：下落与停摆改为 `1380ms`，约在 `66%` 形成一次重落点；随后只保留一次小回弹和一次更小的压缩。镜头总时长为 `1850ms`，卷帘延迟 `1360ms`、时长 `440ms`。
- 证据：`08-weighted-motion-contact-sheet.png`。

### [P1] 档口主体太小，蓝色空区抢走注意力

- 修改前：银色窗口约占场景 `94%`，招牌和人物偏小，左右切换按钮也偏厚重。
- 影响：银色档口像贴在卡片中的一张小图，而不是主页的视觉主角。
- 修复：档口宽度提升至场景 `100%`；招牌增至主体 `46%` 宽；厨师和菜单随服务区一起放大；卡片水平内缩从 `2.35rem` 收至 `1.75rem`；左右按钮缩小。
- 证据：`01-current-ui.png` 与 `07-final-stable.png`。

### [P1] “营业 / 打烊”只换文字，卷帘不动

- 修改前：按钮会改变 `aria-label` 和状态文案，但银色卷帘一直保持打开。
- 影响：最明显的场景操作没有视觉反馈，按钮语义与画面矛盾。
- 修复：每张汉堡卡片增加 `is-business-open` 状态；营业时卷帘升起，打烊时卷帘落下。进场动画只在营业状态下于主体停稳后打开卷帘。
- 浏览器验证：
  - 营业：`aria-pressed="true"`，卷帘矩阵 Y 为约 `-162px`。
  - 打烊：`aria-pressed="false"`，卷帘矩阵 Y 为 `0`。

## 必查表面

### 字体与排版

- 使用现有中文系统黑体与粗字重，没有引入海报字体或 AI 图像内嵌文字。
- “汉堡档口 / 现做 · 热乎 · 出餐”在移动宽度下不换行、不截断。
- HUD、主页标题、玩法标签、档口招牌、菜单与营业状态形成清楚的字号阶梯。

### 间距与布局

- 档口水平居中并占满可用场景宽度。
- 厨师落在视觉中线；菜单、后厨、出餐台形成清楚的上中下三段。
- 两侧切换按钮不遮挡人物、菜单或营业按钮。
- 顶部空区只用于提线进入，不再增加操偶横杆或装饰性机械件。

### 色彩与素材

- 暖黄、深棕、银灰、快餐红、菜单深绿和奶油白与参考方向一致。
- 银色边框使用平面明暗，不含镜面高光、霓虹和油亮塑料效果。
- 后厨与人物继续使用已确认素材，由扁平银色框体统一，不新增 AI 海报图。
- 车头、车门、车轮、轮眉和道路语义均未重新出现。

### 图标与控件

- HUD 与底部导航维持同一套粗描边和圆角语言。
- 左右切换按钮缩小后仍有清楚的焦点轮廓和可点击面积。
- 营业按钮的文案、`aria-pressed` 和卷帘状态一致。

### 响应与可访问性

- 移动视口下无重叠、溢出或被裁切的关键内容。
- 交互仍使用原生按钮，保留可读 `aria-label` 与 `:focus-visible`。
- 方向键、左右按钮和左右卡片返回均可重新触发汉堡档口进场。

## 交互与状态验收

- 从汉堡向右进入寿司，再向左返回：汉堡相机获得 `is-arriving`。
- 从汉堡向左进入寿司，再向右返回：汉堡相机同样获得 `is-arriving`。
- 进场结束后 `is-arriving` 和 `is-truck-replaying` 被清除，重播与营业按钮重新出现。
- 营业按钮能真实开合卷帘，且状态文案同步。
- 浏览器控制台：`0` 条 error。
- Node 自动测试：`30/30` 通过。
- `git diff --check`：通过。

## 剩余 P3

- 浏览器的移动视口捕获会被平铺，截图只能作为构图和状态证据；真机仍应补一次触摸滑动手感验收。
- 厨师目前仍是完整图片。下一阶段如做提线木偶人物，只拆人物关节，不改变汉堡与食材的现有装配方式。

## 最终结论

当前版本已经把视觉焦点集中到完整银色出餐窗口，并把“漂浮下落”改成可读的重力落位；左右返回、重播和营业卷帘三类核心交互与画面状态一致。

final result: passed
