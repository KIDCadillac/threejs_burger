# 银色汉堡餐车 Design QA

最后检查：2026-07-27

## 比较证据

- 用户指出的旧整车：`output/burger-truck-silver/before-arrival.png`
- 用户指出的旧近景：`output/burger-truck-silver/before-final.png`
- 新版银色整车入场：`output/burger-truck-silver/browser-arrival-phone.png`
- 新版厨师出餐近景：`output/burger-truck-silver/browser-final-phone.png`
- 前后四宫格：`output/burger-truck-silver/before-vs-silver.png`
- 当前栅格素材总览：`output/burger-truck-silver/silver-assets.png`
- 原游戏风格参考：`docs/art-direction/reference-flat-burger-game.png`

## 本轮验收目标

1. 餐车必须是完整、统一的银色餐车，不再由黄色车头和奶油色车厢拼接。
2. 第一阶段必须看到整台车开进卡片，而不是一张局部截图直接放大。
3. 第二阶段镜头必须落在出餐口中央，招牌、厨师脸、双手和汉堡形成清晰中轴。
4. 厨师必须完整可见，不能只剩两只手。
5. 从汉堡页左右滑到其他地图，再滑回汉堡页，入场动画必须自动重播。

## Findings

当前没有未解决的 P0、P1 或 P2。

- 构图：整车状态可见银色驾驶室、统一车身、两个车轮和完整服务窗口；最终近景以厨师和汉堡为中心，没有黑色圆形遮挡物。
- 美术一致性：车体、车轮、卷帘和服务窗口采用同一套粗深棕描边、低渐变、有限色板；银色是暖灰银，不使用摄影级金属反射。
- 人物中心：正面厨师的帽子、脸、围裙、双手和汉堡均在窗口中轴；不再使用“只有手臂”的旧出餐图。
- 动画：整车从右侧驶入，车轮滚动，车身刹停，卷帘上升，菜单翻转，随后镜头推进到服务窗口。
- 回播：离开汉堡地图再返回时，活动卡片会自动调用 `replayActiveTruckArrival()`；回播期间左右箭头和营业控件隐藏，结束后恢复。
- 可访问性：营业按钮继续同步 `aria-pressed`；装饰图使用空 `alt`；`prefers-reduced-motion` 降级规则保留。
- 资源：活动汉堡卡片共 12 个图片节点，全部 `complete` 且 `naturalWidth > 0`；新 WebP 无缺图、拉伸或透明边缘黑底。

## 浏览器回归

- 本地 URL：`http://127.0.0.1:4173/`
- 从汉堡页滑到寿司页：成功。
- 从寿司页滑回汉堡页：自动回播在汉堡页激活后约 450 ms 触发。
- 回播结束后控件恢复：成功。
- 营业状态：`false → true → false` 正常切换。
- 页面控制台错误：0。

## 历史问题与修复

### P1：黄色拼装车

旧版把黄色驾驶室、奶油色车厢和红白卷帘拼在一起，车身语言不统一，也不符合用户指定的银色餐车。

修复：生成并接入一套连贯的银色车身、匹配车轮和银色卷帘；只把车轮、卷帘、菜单和服务窗口拆成需要独立运动的层。

### P1：近景偏位且没有人物

旧版镜头落在窗口上沿和招牌，画面中心只有手臂与汉堡。

修复：改用正面居中的厨师窗口，并把镜头轴锁在“招牌—厨师—汉堡”中心。

### P1：滑回汉堡页没有动画

旧版只有首次加载和手动重播会触发动画，轮播切回后停在结束帧。

修复：轮播完成并重置缓冲卡片后，在下一次绘制主动重播当前汉堡卡片，避免重置时序吞掉动画。

### 已淘汰方案

- 单张整车截图缩放。
- 黑色大轮胎挡住出餐口。
- 软陶 3D、摄影金属反射和高光厚重的 AI 素材拼贴。
- CSS 手工拼装黄色车头与奶油车厢。

final result: passed
