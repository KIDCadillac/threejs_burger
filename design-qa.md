# 提线木偶小剧场 Design QA

日期：2026-07-30

final result: passed

## 对比目标

- 视觉真值：`docs/art-direction/reference-flat-burger-game.png`
- 被否决旧版：`output/suspended-booth/final-mobile.png`
- 浏览器终态：`output/booth-redesign-audit/03-puppet-theatre-final.png`
- 同屏总览：`output/booth-redesign-audit/04-reference-before-after.png`
- 动画关键帧：`output/booth-redesign-audit/08-animation-contact-sheet.png`
- 档口专注编辑器：`output/booth-redesign-audit/09-editor-theatre-final.png`
- 本地验收地址：`http://127.0.0.1:4173/?visualQa=1&motion=theatre2`

## 视口与归一化

- 浏览器原始截图：`5120 × 2880 px`，Codex 内置浏览器返回的平铺桌面捕获。
- 实现归一化：从首个平铺视口裁出居中的 `420 × 896 px` 移动页面，并补齐为 `420 × 900 px`。
- 参考图原始尺寸：`974 × 310 px`；其中手机画面为 `127 × 241 px`，仅作为扁平描边、配色与密度的风格真值，不按像素克隆内容。
- 页面 CSS 宽度：中心移动应用约 `382 px`；浏览器密度按 `1×` 检查。
- 状态：汉堡卡片居中、档口吊降完成、卷帘打开、开店按钮可用。

## 对比历史

### 第 0 轮：被否决版本

- [P1] 档口像从车身硬裁下来的铁皮。
  - 证据：旧版 `silver-service-booth-frame.png` 只有车顶、两根侧框和一条下沿，没有完整底座。
  - 影响：主体没有完整轮廓，无法读成木偶戏布景。
- [P1] 横杆和 V 形长绳像起重机。
  - 证据：旧版同屏图中木杆横在蓝色空区，两根绳向外大角度张开。
  - 影响：机械吊装感压过了手工提线木偶感。
- [P2] 终态过扁、过低，顶部空区发散。
  - 证据：旧档口宽高比约 `2.16:1`，人物和后厨被压在一条窄带中。
  - 影响：视觉中心不稳，厨师和出餐动作太小。

### 修复

- 新建 `silver-puppet-booth-frame.png`：加入完整圆角外框、红黄顶篷、交替布檐、透明出餐口、厚银色底座和纸板分缝。
- 新建 `puppet-booth-strings.png`：取消横杆，只保留两根近乎垂直、带轻微手工偏差的短提线。
- 档口宽高比改为 `4:3`，服务区与厨师整体增高，招牌整合进顶篷。
- 动画改为整块舞台受重力下落、轻微过冲、摆幅递减、稳定后开卷帘；静止时仅保留极轻微同相摆动。

### 第 1 轮：修复后

- 后置证据：`04-reference-before-after.png`。
- 动画证据：`08-animation-contact-sheet.png`。
- 结果：此前 P1/P2 均已消除，没有新增 P0/P1/P2。

## 必查表面

### 字体与排版

- 延续现有中文系统黑体与粗字重，标题、HUD、招牌和小字层级与参考图一致。
- “汉堡档口 / 现做 · 热乎 · 出餐”在 `382 px` 页面宽度下不换行、不截断。
- 没有引入新的海报字体或 AI 图像内嵌文字。

### 间距与布局节奏

- 档口水平居中，左右提线与顶角铜环同轴。
- 厨师落在画面中线，菜单、窗口、底座形成清楚的上中下三段。
- 两侧切换按钮不遮挡人物、菜单或营业按钮。
- 顶部留白现在承担提线长度，属于有功能的舞台空间。

### 色彩与视觉令牌

- 暖黄背景、深棕描边、银灰、快餐红、奶油白和少量黄色与参考方向一致。
- 银色采用平面明暗分区，没有镜面反射、霓虹或塑料高光。
- 后厨的细节图像被扁平外壳包住，形成“AI 图像内容 × 手工 UI 框架”的混合，而不是整张 AI 海报。

### 图像与素材质量

- 新框体为 `960 × 720` 透明 PNG，移动端缩小后边缘清楚，没有轮廓白边或透明晕圈。
- 提线为 `764 × 790` 透明 PNG，线端与两只铜环对齐。
- 后厨、菜单和卷帘继续使用项目原始图片，没有占位图、CSS 画车或内联 SVG。

### 文案与内容

- 保留“汉堡小馆”“自由练习”“今日菜单”“汉堡档口”和营业状态，语义连续。
- 车头、车轮、道路和车辆行驶相关文案均未重新出现。

## 交互与状态验收

- 点击营业按钮后，`aria-label` 从“已打烊”切换为“营业中，点击关门打烊”。
- 点击下一张地图后进入寿司卡片。
- 从寿司向左返回汉堡，当前汉堡相机会重新获得 `is-arriving`，完整吊降再次播放。
- 动画结束后 `is-arriving` 被清除，重播按钮重新可见。
- 编辑器只列出 8 个档口图层，完整档口在中央画布内可见；浏览器控制台 `0` 错误。
- Node 自动测试：`30/30` 通过。
- `git diff --check`：通过。

## 剩余 P3

- 提线在非常亮的屏幕上仍然较细；如果真机上不够清楚，可把最终不透明度从 `.62` 提到 `.68`。
- 厨师目前是完整图片，下一轮做人物提线动作时，应只拆人物关节，不改汉堡装配逻辑。

## 最终结论

当前实现已经从“被裁下来的车体”变成完整、可读、扁平的提线木偶小剧场；视觉方向、构图、素材质量和核心交互均达到本轮验收要求。

final result: passed
