# 旋转料理主页 + 第一人称汉堡 Design QA

验收日期：2026-08-01

分支：`main`

## 本轮设计决定

- 主页不再使用卡片、餐车、车头、轮子或卷帘档口。
- 中央只有一个持续旋转的料理模型。
- 左右手势切换料理主题，当前为汉堡与寿司。
- 上下手势切换玩法，当前为自由练习、复刻对决和双人制作。
- 汉堡制作页保持第一人称台面，木偶感只出现在短暂协助拿食材的小手。

## 可见问题与修复

### [P0] 长手臂挡住汉堡

旧实现使用完整前臂贴图，从底部一直延伸到餐盘中央，遮挡主要操作目标，而且透视与 3D 台面不一致。

已修复：

- 删除料理页对 `puppet-chef-arm-left.png` 和 `puppet-chef-arm-right.png` 的加载。
- 新增只有手套、短袖口和腕关节的透明 PNG。
- 显示宽度限制在操作区约 10%，放置状态停在餐盘边缘。
- 放下后 360 ms 内退场。

证据：`output/burger-gameplay-qa-2026-08-01/05-hand-before-after.png`

### [P0] 左侧食材错误使用右手

原始手套 PNG 是右手。旧样式只把它旋转到左侧，因此虽然出手位置正确，拇指方向仍然暴露为右手。

已修复：

- 左侧出手先把原始右手在素材自身坐标中镜像成左手，再旋转朝向左侧食材。
- 右侧出手保留原始右手，并以相反角度朝向右侧酱料。
- 单元测试同时锁定左手负镜像矩阵、右手非镜像矩阵和食材槽位到出手侧的映射。

证据：`output/handedness-fix-2026-08-01/04-left-right-validation.png`

### [P0] 茄汁瓶悬空，手只在挤完后闪现

旧实现只监听酱料提交事件，右手在松手后才播放约 420 ms，拿起、移动和挤压阶段都看不到手，因此瓶子像自己悬空工作。

已修复：

- 交互控制器新增调料瓶 `start / move / end` 生命周期。
- 右手从按下调料瓶开始保持可见，并使用瓶身的实时 3D 投影坐标逐帧跟随。
- 酱料提交不会打断握持状态；松手后才进入 220 ms 退场。
- 浏览器实际拖动后记录到 `sauce-hold → sauce-release`，手部坐标从右侧料架移动到餐盘上方。

### [P0] 全局胶囊误删实体瓶，玩家无法按瓶位管理酱料

上一版把右侧三只瓶子隐藏，只留下底部一个全局胶囊。这曲解了设计：玩家需要看到实体调料架，并对每一只瓶子独立换酱、指定和取用。

已修复：

- 料理镜头继续锁定，单指不能误旋转、双指不能误缩放。
- 右侧三只真实三维瓶子始终可见，底部全局胶囊从当前 HTML 与应用接线中移除。
- 某只瓶水平滑动达到 18 px 且横向占优时，只切换该槽；实测第二槽从芥末改成特调，第一、第三槽不受影响。
- 某只瓶静止长按约 360 ms，选择器贴着该瓶向左展开，以三个瓶形图标指定番茄、芥末或特调。
- 某只瓶向上拖达到 20 px 且纵向占优时，按 `slotId` 拿起这一只真实瓶；即使多个槽装同一种酱也不会拿错瓶。
- 最终松手未命中汉堡、Esc、页面隐藏、暂停或丢失指针都会取消并归还原槽，不会误提交。
- 轻点不会换酱或拿瓶，只显示简短操作提示；键盘 Enter 可打开同一个图标选择器。

同状态证据：

- `output/condiment-rack-remap-2026-08-01/01-before-empty-docks-1280.png`：旧版同为 1280×900 视口，只有三个空托盘和底部全局胶囊。
- `02-after-three-bottles.png`：三只实体瓶恢复，旧胶囊消失。
- `03-after-slot2-swipe.png`：只切第二槽后出现两只特调瓶，说明槽位允许重复且没有全局替换。
- `04-after-long-press-picker.png`：三图标选择器锚定第一只瓶，不遮挡汉堡中心。
- `04b-after-icon-assign.png`：选择芥末图标后只改变第一槽。
- `06-pickup-contact.png → 07-carry-to-patty.png → 08-release-contact.png → 09-released.png → 10-settled.png`：同一次手势中，右手与第一槽番茄酱瓶一起离架、挤酱、朝原槽退场并完成 3/6。

浏览器实测记录为 `sauce-right-1 → patty → ketchup`；拿取路径擦过其它层仍只提交最终松手层。

### [P0] 普通食材自己飞，手只播放固定姿势

旧实现只根据 `selection / drop-intent / drop-layer` 把手切到几个写死位置。真实食材沿玩家拖动轨迹移动时，手不会跟着它，因此仍然像食材悬空飞到餐盘。

已修复：

- 普通食材新增 `start / move / end` 生命周期，和调料瓶使用同一套“真实物体投影驱动手”原则。
- 每个移动采样都从 Three.js 世界坐标投影为屏幕归一化坐标，再写入 `--hand-ingredient-x / --hand-ingredient-y`。
- `drop-intent` 与 `drop-layer` 在有效手势结束前不会打断握持状态。
- 左侧上层面包实测使用镜像左手；右侧洋葱实测使用原始右手；两条轨迹都包含连续移动采样。
- 放下后进入 `ingredient-release`，约 220 ms 退场。

证据：`output/burger-gauntlet-ingredient-hand-2026-08-01/`

### [P0] 移动窄屏订单条压住食材命中区

旧的通用移动样式把目标卡和订单参考层绝对定位在料理台上方；第一人称样式只改了颜色与尺寸，没有重置定位，约 480 CSS px 时第一步下层面包的投影点被订单元素截获。

已修复：

- 700 px 以下明确把目标卡与订单参考恢复为正常文档流。
- 料理台恢复自身 4:3 命中区域，Canvas 高度重新跟随容器。
- 窄屏重载后食材投影点不再落在订单 DOM 上。

证据：`09-mobile-480-start.png` 对比 `09-mobile-layout-fixed.png`，并在修复后实际完成下层面包 1/6。

### [P0] 首页卡片与餐车叙事抢走游戏主题

旧首页先展示餐车结构、卷帘和相邻卡片，玩家看不到最重要的“做什么料理”。

已修复：

- 玩家首屏仅展示旋转汉堡或旋转寿司。
- 取消相邻卡片渲染，解决切换时中间叠卡。
- 主题标题、配色和按钮随食物一起切换。

证据：`output/burger-gameplay-qa-2026-08-01/03-home-before-after.png`

### [P1] 横向与纵向手势职责不清

已固定交互语法：

- 左滑 / 右滑：只切换汉堡与寿司。
- 上滑 / 下滑：只切换玩法。
- 键盘左 / 右与上 / 下完全对应。
- 浏览器实测在寿司主题上上滑，玩法从 `1/3 自由练习` 变为 `2/3 复刻对决`，主题不变。

### [P1] 汉堡只是自由堆叠，没有完成感

已固定为六步订单，番茄酱纳入真实进度；完成后显示评分与金币，并防止重复结算。

证据：`output/burger-gameplay-qa-2026-08-01/06-cooking-finish.png`

## 浏览器验收

- 主页汉堡模型持续旋转。
- 左滑成功切到寿司，标题、色彩和主按钮同步变化。
- 寿司主题上上滑成功切到复刻对决，没有切回汉堡。
- 经典汉堡六步可以完整完成。
- 牛肉饼放置时小手短暂出现，不遮挡汉堡主体。
- 左侧面包与配料由左手进入，右侧酱料由右手进入，拇指方向对应各自手型。
- 拿起、拖动和挤压番茄酱时右手全程贴着瓶身，松手后才退场。
- 右侧第二只瓶左右滑后只更新第二槽；三只实体瓶始终可见。
- 长按选择面板贴着目标瓶展开；点击瓶形图标只改目标槽。
- 第一只番茄酱瓶上拨到牛肉饼，订单从 2/6 进入 3/6，调试轨迹记录目标层为 `patty`。
- 1280×900 验收中，选择面板、汉堡中心、提示和底部工具栏互不遮挡。
- 拿起、拖动普通食材时正确侧的手逐帧跟随；右侧洋葱与左侧上层面包都完成了 `start → move → end` 实测。
- 主页不自动弹出签到层。
- 控制台无新增 error；Three.js 旧页面遗留警告不影响运行。

## 自动验收

- Node：81/81 通过。
- 缓存入口链：`20260802-gameplay30` 全链一致。
- `git diff --check`：交付前再次执行。
- 页面结构测试确认首页不再包含 `.home-map-slide` 或 `.burger-truck`。
- 交互测试确认水平轴使用 `resolveSwipe → moveTheme`，垂直轴使用 `resolveModeSwipe → moveMode`。

## 当前限制

- 寿司是主页预览，不是可玩的订单。
- 手是 2D 素材对齐到 3D 投影，不是骨骼 IK；后续仍需为不同尺寸食材校准独立握住点。
- UI 编辑器中仍有旧餐车时代的专用名称，下一轮需要清理。

final result: passed
