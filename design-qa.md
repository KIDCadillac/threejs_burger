# 旋转料理主页 + 第一人称汉堡 Design QA

## 2026-08-13：程序化 3D 厨师手区分左右与物品抓握

本轮只收紧汉堡料理纵切，不再继续改主页。玩家可见结果是：料理台、食材、酱料瓶和双手全部由 Three.js 几何体实时绘制；页面不加载手部 PNG、截图预览或纹理图片。

- 两只手分别建模，不再镜像同一张右手图片；左手拇指位于画面内侧，右手拇指反向对应。木质腕/前臂已经删除，改成奶油白食品手套、短白厨师袖和红色防污袖口。
- 面包使用托握、肉饼使用夹持、酸黄瓜使用拇指/食指精细捏取、洋葱使用兜捏、调料瓶使用四指环握；挤酱时手指随压力继续收紧。
- 抓握锚点以物体 3D 包围盒顶面为准，手始终处于物品可见上层；深度测试仍保留，避免退化成贴在画面上的平面手。
- 食材按 `reach → grip → carry → release → withdraw` 连续动作完成抓取；食材在握住前不会自行移动。
- 左侧面包与前两格后排配料由左手取用，后两格配料和右侧调料瓶由右手取用。
- 松手时保留真实释放点；垂直方向按重力加速落下，首次接触后只执行一次材质相关压缩与回弹。
- 面包、肉饼、酸黄瓜、洋葱等使用不同柔顺度；下方堆叠按深度传递受力，不再整座汉堡统一缩放。
- 重做订单后，以真实浏览器指针从 `0/6` 完整做到 `6/6`：`下层面包 → 牛肉饼 → 番茄酱（目标层 patty）→ 酸黄瓜 → 洋葱碎 → 上层面包`。
- 运行时 `document.images.length === 0`；手部调试标识为 `procedural-3d`，左右两套手均已创建。
- 390 × 844 视口覆盖测试无横向溢出，料理台、餐盘、配料架与完成按钮保持可读。

确定性验收：Node `85/85`、skill repo check、`git diff --check`、入口缓存链和运行时图片引用扫描全部通过；缓存标记为 `20260813-hands34`。真实浏览器从 `0/6` 做到 `6/6`，运行时 `img=0`，控制台日志为 `[]`。

发布验收：玩法提交 `f0e5823` 已推送 `main`；GitHub Pages 入口 `https://kidcadillac.github.io/threejs_burger/cooking.html?recipe=classic-beef&debug=1&deploy=f0e5823` 返回 200，入口和手部模块均实际加载 `20260813-hands34`。线上源码含白厨师袖和五种抓握且不含木杆前臂；本地真实浏览器运行态为 `procedural-3d`、双手实例数 2、运行时图片数 0、控制台错误 0。

证据目录：`output/burger-realism-3d-2026-08-13/`。本轮新增 `37-left-right-distinct-chef-grips.png`（五种抓握同视角对照）和 `38-hands34-complete-6-of-6.png`（实机六步完成帧）。更早的重复截图只保留为历史，不参与完成判定。

验收日期：2026-08-02

分支：`main`

## 本轮设计决定

- 主页不再使用卡片、餐车、车头、轮子或卷帘档口。
- 中央只有一个持续旋转的料理模型。
- 左右手势切换料理主题，当前为汉堡与寿司。
- 上下手势切换玩法，当前为自由练习、复刻对决和双人制作。
- 汉堡制作页保持第一人称台面，木偶感只出现在短暂协助拿食材的小手。

## 可见问题与修复

### [P0] 长手臂挡住汉堡

旧实现使用完整前臂贴图，从底部一直延伸到餐盘中央，遮挡主要操作目标，而且透视与 3D 台面不一致。

已修复：

- 删除料理页对 `puppet-chef-arm-left.png` 和 `puppet-chef-arm-right.png` 的加载。
- 新增只有手套、短袖口和腕关节的透明 PNG。
- 显示宽度限制在操作区约 10%，放置状态停在餐盘边缘。
- 放下后 360 ms 内退场。

证据：`output/burger-gameplay-qa-2026-08-01/05-hand-before-after.png`

### [P0] 左侧食材错误使用右手

原始手套 PNG 是右手。旧样式只把它旋转到左侧，因此虽然出手位置正确，拇指方向仍然暴露为右手。

已修复：

- 左侧出手先把原始右手在素材自身坐标中镜像成左手，再旋转朝向左侧食材。
- 右侧出手保留原始右手，并以相反角度朝向右侧酱料。
- 单元测试同时锁定左手负镜像矩阵、右手非镜像矩阵和食材槽位到出手侧的映射。

证据：`output/handedness-fix-2026-08-01/04-left-right-validation.png`

### [P0] 茄汁瓶悬空，手只在挤完后闪现

旧实现只监听酱料提交事件，右手在松手后才播放约 420 ms，拿起、移动和挤压阶段都看不到手，因此瓶子像自己悬空工作。

已修复：

- 交互控制器新增调料瓶 `start / move / end` 生命周期。
- 右手从按下调料瓶开始保持可见，并使用瓶身的实时 3D 投影坐标逐帧跟随。
- 酱料提交不会打断握持状态；松手后才进入 220 ms 退场。
- 浏览器实际拖动后记录到 `sauce-hold → sauce-release`，手部坐标从右侧料架移动到餐盘上方。

### [P0] 实体瓶手势方向互相打架，胶囊选择不是连续轮盘

上一版虽然恢复了右侧三只实体瓶，但“左右都换酱、向上拿瓶、长按后再点图标”的语法不符合玩家预期：拿瓶和选择器会抢方向，长按也不像保持按住拨动的胶囊轮盘。

已修复：

- 料理镜头继续锁定，右侧三只真实三维瓶子始终可见，底部全局胶囊不恢复。
- 从某只瓶向左拖达到 20 px 且横向占优时，按 `slotId` 直接拿起这只瓶；继续拖到汉堡上松手即可挤酱。
- 从某只瓶向右滑达到 18 px 时，只快速循环这一槽；线上实测第二槽从芥末改成特调，第一、第三槽不受影响。
- 某只瓶静止长按约 360 ms 后弹出竖向胶囊轮盘；同一指针保持按下，每向上约 44 px 转一格，松手停在哪格就确认哪种酱。
- 长按允许最多 10 px 静止漂移；超过容差会立即清除计时器并取消长按资格，斜向移动不会误弹轮盘，后续仍可继续解析成左拖或右滑。
- 在汉堡外松手、Esc、页面隐藏、暂停或丢失指针都会取消并归还原槽，不会误提交。
- 键盘保留可访问路径：右方向键快切，Enter / Space 打开轮盘，上下方向键转动，Enter 确认。

同状态证据：

- `output/condiment-gesture-remap-2026-08-02/01-before-focus.png`：三只实体瓶与料理台改动前基线。
- `02-right-swipe-focus.png`：只把第二槽从芥末切成特调，其他槽保持原样。
- `03-hold-up-roulette-house-focus.png`：第一槽静止长按后保持按住向上拨动，竖向胶囊停在特调。
- `05-left-drag-committed-focus.png`：第一槽向左取瓶并拖到牛肉饼挤酱，订单从 2/6 进入 3/6。
- `06-mobile-roulette.png`：移动窄屏中轮盘完整位于料理画布内。
- `08-pages-full-gameplay31.png`：提交 `68d2daf` 部署后的 GitHub Pages 最终 3/6 全页画面。
- `browser-verification.json`：线上连续手势、缓存链、最终槽位与页面控制台 `[]` 的完整记录。
- `blind-review.md`：首次盲审因缺少 10 px 漂移容差判 FAIL；补实现和斜向漂移回归测试后复审 PASS。

浏览器连续实测为“第二槽右滑 → 第一槽长按上滑轮盘 → 第一槽右滑复位 → 第一槽向左拖到牛肉饼并松手”；最终只在目标层提交番茄酱。

### [P0] 普通食材自己飞，手只播放固定姿势

旧实现只根据 `selection / drop-intent / drop-layer` 把手切到几个写死位置。真实食材沿玩家拖动轨迹移动时，手不会跟着它，因此仍然像食材悬空飞到餐盘。

已修复：

- 普通食材新增 `start / move / end` 生命周期，和调料瓶使用同一套“真实物体投影驱动手”原则。
- 每个移动采样都从 Three.js 世界坐标投影为屏幕归一化坐标，再写入 `--hand-ingredient-x / --hand-ingredient-y`。
- `drop-intent` 与 `drop-layer` 在有效手势结束前不会打断握持状态。
- 左侧上层面包实测使用镜像左手；右侧洋葱实测使用原始右手；两条轨迹都包含连续移动采样。
- 放下后进入 `ingredient-release`，约 220 ms 退场。

证据：`output/burger-gauntlet-ingredient-hand-2026-08-01/`

### [P0] 移动窄屏订单条压住食材命中区

旧的通用移动样式把目标卡和订单参考层绝对定位在料理台上方；第一人称样式只改了颜色与尺寸，没有重置定位，约 480 CSS px 时第一步下层面包的投影点被订单元素截获。

已修复：

- 700 px 以下明确把目标卡与订单参考恢复为正常文档流。
- 料理台恢复自身 4:3 命中区域，Canvas 高度重新跟随容器。
- 窄屏重载后食材投影点不再落在订单 DOM 上。

证据：`09-mobile-480-start.png` 对比 `09-mobile-layout-fixed.png`，并在修复后实际完成下层面包 1/6。

### [P0] 首页卡片与餐车叙事抢走游戏主题

旧首页先展示餐车结构、卷帘和相邻卡片，玩家看不到最重要的“做什么料理”。

已修复：

- 玩家首屏仅展示旋转汉堡或旋转寿司。
- 取消相邻卡片渲染，解决切换时中间叠卡。
- 主题标题、配色和按钮随食物一起切换。

证据：`output/burger-gameplay-qa-2026-08-01/03-home-before-after.png`

### [P1] 横向与纵向手势职责不清

已固定交互语法：

- 左滑 / 右滑：只切换汉堡与寿司。
- 上滑 / 下滑：只切换玩法。
- 键盘左 / 右与上 / 下完全对应。
- 浏览器实测在寿司主题上上滑，玩法从 `1/3 自由练习` 变为 `2/3 复刻对决`，主题不变。

### [P1] 汉堡只是自由堆叠，没有完成感

已固定为六步订单，番茄酱纳入真实进度；完成后显示评分与金币，并防止重复结算。

证据：`output/burger-gameplay-qa-2026-08-01/06-cooking-finish.png`

## 浏览器验收

- 主页汉堡模型持续旋转。
- 左滑成功切到寿司，标题、色彩和主按钮同步变化。
- 寿司主题上上滑成功切到复刻对决，没有切回汉堡。
- 经典汉堡六步可以完整完成。
- 牛肉饼放置时小手短暂出现，不遮挡汉堡主体。
- 左侧面包与配料由左手进入，右侧酱料由右手进入，拇指方向对应各自手型。
- 拿起、拖动和挤压番茄酱时右手全程贴着瓶身，松手后才退场。
- 右侧第二只瓶向右滑后只更新第二槽；三只实体瓶始终可见。
- 长按竖向胶囊贴着目标瓶展开；保持按住向上拨动后，松手只改目标槽。
- 第一只番茄酱瓶向左拖到牛肉饼，订单从 2/6 进入 3/6，调试轨迹记录目标层为 `patty`。
- 1280×900 与移动窄屏验收中，轮盘、汉堡中心、提示和底部工具栏互不遮挡。
- 拿起、拖动普通食材时正确侧的手逐帧跟随；右侧洋葱与左侧上层面包都完成了 `start → move → end` 实测。
- 主页不自动弹出签到层。
- 控制台无新增 error；Three.js 旧页面遗留警告不影响运行。

## 自动验收

- Node：84/84 通过。
- 缓存入口链：`20260802-gameplay31` 全链一致。
- `git diff --check`：交付前再次执行。
- 页面结构测试确认首页不再包含 `.home-map-slide` 或 `.burger-truck`。
- 交互测试确认水平轴使用 `resolveSwipe → moveTheme`，垂直轴使用 `resolveModeSwipe → moveMode`。

## 当前限制

- 寿司是主页预览，不是可玩的订单。
- 手是 2D 素材对齐到 3D 投影，不是骨骼 IK；后续仍需为不同尺寸食材校准独立握住点。
- UI 编辑器中仍有旧餐车时代的专用名称，下一轮需要清理。

final result: passed
# 2026-08-13 料理真实性 QA：程序化 3D 木偶手

## 结论

本轮把“料理页不能出现截图或图片”设为硬门。料理运行时已从 2D PNG 手改为程序化 Three.js 手，并把放置动作从吸附弹窗感改为真实释放点下落与接触承重。

## 视觉与动作检查

- 运行时 DOM 中图片数量为 0；没有手部贴图、截图预览或纹理加载器。
- 木偶手在最小验收视口仍能辨认白手套、四指与拇指、红袖口、木质腕/前臂关节。
- 食材抓取手由来源槽位决定：左侧面包/配料使用左手，右侧食材及调料使用右手。
- 手在 `start / move / pointer-up / contact` 期间持续绑定同一个 Three.js 对象，接触后才松指并退场。
- 放置时保留释放高度和横向位置；空中阶段不缩放，首次接触时纵向压缩、横向摊开，随后只回弹一次。
- 已叠好的下层会共同受力；压缩量沿堆叠接触链累计，最终恢复权威位置与比例。

## 自动与浏览器验收

- 全仓：80/80。
- `git diff --check`：通过。
- 料理入口缓存：`20260813-gameplay32n` 全链一致。
- 窄屏：480 CSS px 宽真实拖拽进入 1/6，料理台、餐盘、食材和底部操作区未互相遮挡。
- 浏览器：错误层隐藏，控制台 errors 为 `[]`。

证据：`output/burger-realism-3d-2026-08-13/`。
