# 汉堡餐车扁平混合版 Design QA

最后检查：2026-07-27

## 比较证据

- Source visual truth：`docs/art-direction/reference-flat-burger-game.png`
- 整车入场：`output/burger-truck-hybrid/browser-arrival-phone.png`
- 最终出餐近景：`output/burger-truck-hybrid/browser-final-phone.png`
- 三联同屏比较：`output/burger-truck-hybrid/reference-vs-hybrid.png`
- AI 插画槽位总览：`output/burger-truck-hybrid/hybrid-raster-assets.png`

## 归一化

- 原参考图：980 × 342 px；比较时裁出左侧手机画面并等比放入面板。
- 浏览器手机区域：372 × 1080 px。
- 三联比较画布：1152 × 790 px。
- 状态一：整车已经进入卡片，卷帘关闭，完整车头、车身和两只轮子可见。
- 状态二：卷帘打开，镜头聚焦招牌、翻转菜单和出餐窗口，重播与营业控件恢复。

## Findings

当前没有未解决的 P0、P1 或 P2。

- 布局与构图：入场状态完整显示餐车；最终状态只保留用户要求的局部出餐位置。左右地图箭头、顶部 HUD 和底部导航未被镜头覆盖。
- 字体与文案：沿用现有中文标题、店名、状态和按钮层级。招牌文字由页面排版控制，没有生成图乱码。
- 色板：奶油黄、番茄红、芥末黄、蓝绿色和深棕描边与原游戏方向一致。
- 轮廓与表面：车身、轮胎、雨棚、菜单框和招牌回到统一粗描边、低渐变的扁平结构；没有写实金属轮胎和大面积塑料高光。
- 图片质量：AI 只出现在服务窗口和三张菜单图中。四个 WebP 素材尺寸有效，没有缺图、拉伸、透明边缘黑边或明显压缩破损。
- 汉堡可读性：最终近景可以分辨面包、生菜、番茄、芝士、肉饼和底层面包，主体未被黑色圆形或按钮遮挡。
- 状态与交互：整车驶入、车轮滚动、刹停、卷帘打开、菜单翻转和镜头推进分别运行。
- 可访问性：装饰图使用空 `alt`；营业按钮同步 `aria-pressed` 和可访问名称；按钮保留项目已有焦点样式；减少动态效果规则继续生效。
- 响应性：手机卡片内未出现横向溢出，最终近景未裁掉招牌、菜单主信息或汉堡主体。

## Comparison history

### Iteration 1：单张整车 AI 截图

- [P1] 整车只有一张图，动画本质是截图位移和放大。
- [P1] 黑色大轮胎挡住出餐窗口，最终焦点错误。
- 修复：拆掉单图方案，重新建立独立动画层级。

### Iteration 2：分层软陶 3D 资产

- [P1] 车身、轮胎、灯牌和汉堡过于写实、油亮，与原游戏扁平风格割裂。
- [P2] 页面像在展示一套 AI 素材，而不是原游戏中的餐车。
- 修复：车身结构回到粗描边扁平语言；AI 只保留为出餐窗口和菜单插画。

### Iteration 3：菜单仍偏 3D

- [P2] 汉堡、薯条和饮料小图仍有明显软陶高光，与菜单框不一致。
- 修复：三张菜单图重新生成并压缩为 520 × 520 的平涂粗描边版本。
- 复测：`output/burger-truck-hybrid/reference-vs-hybrid.png` 中菜单小图与车身色板一致，未抢过出餐汉堡的主视觉。

## 浏览器验证

- 本地 URL：`http://127.0.0.1:4173/`
- 活动卡片图片节点：7 个，全部 `complete` 且 `naturalWidth > 0`。
- 菜单素材：520 × 520；服务窗口：1300 × 751。
- 营业按钮：`aria-pressed` 按 `false → true → false` 正常切换。
- 重播开始：镜头带 `is-arriving`，重播和营业控件隐藏。
- 重播结束：`is-arriving` 移除，两个控件恢复。
- 页面控制台错误：0。

## Focused region comparison

最重要的局部是最终出餐窗口。与原参考相比，实现保留了粗描边、暖色食物、暗色窗口和红色出餐台；同时增加了用户要求的餐车招牌和翻转菜单。AI 插画被限制在窗口内部，不再改变整个页面的视觉语言。

final result: passed
