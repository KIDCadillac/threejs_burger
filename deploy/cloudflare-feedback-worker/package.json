{
  "name": "burger-feedback-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test ../../tests/github-feedback-worker.test.mjs",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
